- task-01: Reported CI registration evidence in `.loop/findings/task-01-ci-registration.md`. Squash push `7a24607` registered in 3s but automatic run cancelled by workflow concurrency; manual rerun CI job passed, release bot push blocked by active PR-only ruleset. Assumption: report is committed despite unresolved release reliability because rubric requires a blocked diagnosis record.
- task-01 retry: Added GitHub API links for master-history, run-attempt, Actions, and ruleset evidence; successful workflow-run evidence for exact SHA is unavailable, so that criterion remains blocked. Gotcha: do not treat the successful manual CI job as a successful workflow run.
- task-01 retry 2: Re-ran tracker commands; narrowed the finding to the confirmed concurrency cancellation mechanism. Assumption: remove workflow-level concurrency only as a bounded policy mitigation; the unexposed competing request leaves deeper cause unresolved.
- task-01 retry 3: Linked historical successful master-push run `30154658873` for `37f693e…`; preserved timestamped, filtered API excerpts for the 07:30–07:40Z window. Gotcha: a rerun changes the run's final conclusion in listing APIs, so cite attempt 1 for automatic cancellation.
- task-01 retry 4: Corrected registration delay to 3s (`07:35:05Z − 07:35:02Z`) and added plain-language definitions. Gotcha: successful manual CI job remains distinct from a successful automatic workflow run.
- task-01 retry 5: Reconciled the tracker’s 2026-07-25 no-protection assertion with the active PR-only ruleset: environment changed; timing and reason unresolved. Replaced opaque evidence labels with plain language. Gotcha: do not infer who added the ruleset or why.
- task-02: Verified PSR v10.6.1 in disposable history: `fix:` after `v0.12.0` yields `0.12.1`, synchronises both version files, and inserts the changelog after its marker without build, tag, push, or VCS release. No workflow edit: task-01 found no trigger correction required. Gotcha: PSR needs an `origin` remote even for `--print`; use a placeholder remote and disable all publishing flags.
- task-03: Recorded live read-only GitHub API evidence for `marcellovictorino/local-whisper`: the release job's `contents: write` overrides the read default, but the active PR-only `master` ruleset has no bypass actors, so `github-actions[bot]` cannot complete the release commit-and-tag push. Assumption: the user-owned repository has no organisation policy because the owner API identifies `marcellovictorino` as `User` and the organisation endpoint returns 404. Gotcha: branch-protection API 404 does not mean the branch is writable when rulesets apply.
- task-03 retry: Made the workflow-to-token-to-bot chain reproducible with `git show` at base `239e93c`, including token input and bot identity; removed unsourced failure causation. Added plain-language decision and administrator options without changing policy. Gotcha: the base commit is local and GitHub's contents API returns 404 for it. Checks: `just lint`, `just test` (184 passed).
- task-03 retry 2: Added pinned PSR metadata and entrypoint evidence that its configured `github_token` performs the release push; GitHub documentation plus matching GitHub Actions app and bot URLs establish the authenticated actor. Recorded the base-SHA contents API `404` response and a glossary. Gotcha: Git committer fields are metadata, not authentication evidence. Checks: `just lint`, `just test` (184 passed).
